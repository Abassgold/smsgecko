# --- Server ---
NODE_ENV=development
PORT=4000
HOST=0.0.0.0
LOG_LEVEL=info

# Origin allowed to call the API with credentials (the web app).
WEB_ORIGIN=http://localhost:3000

# --- Database ---
# The app reads MONGODB_URI. Local single-node replica set (docker-compose / npm run db:mem):
MONGODB_URI=mongodb://localhost:27017/smsgecko?replicaSet=rs0&directConnection=true
# Or MongoDB Atlas (put real credentials only in .env, which is gitignored):
# MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/smsgecko?retryWrites=true&w=majority
# Optional, not yet used by the app — a separate store for logs:
# MONGO_LOGS_URI=mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/smsgecko_logs?retryWrites=true&w=majority
# --- Auth ---
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_ACCESS_SECRET=dev-access-secret-change-me
JWT_REFRESH_SECRET=dev-refresh-secret-change-me
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d
# Cookie domain/secure — leave COOKIE_SECURE=false for http localhost.
COOKIE_SECURE=false

# AES-256-GCM key (base64, 32 bytes) for encrypting provider credentials at rest.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SETTINGS_ENC_KEY=dev-settings-enc-key-change-me

# --- Email (Resend) ---
# Unset -> verification links are logged instead of sent (fine for local dev / tests).
RESEND_API_KEY=
# Sender address. The default (onboarding@resend.dev) is Resend test mode and only
# delivers to the Resend account owner. Until SMSGecko has its own verified domain,
# send from the already-verified flozap.com.ng domain so mail reaches real inboxes:
EMAIL_FROM=SMSGecko <noreply@flozap.com.ng>
# Base URL the verification link points at (the web app).
APP_URL=http://localhost:3000
EMAIL_VERIFICATION_TTL_MINUTES=5

# --- Payments ---
# Use the simulated payment provider for the `mock`/`qris`/`ewallet` deposit
# methods (deposits confirm instantly). `card` and `crypto_usdt` also fall
# back to it when the Stripe/NowPayments keys below are unset.
PAYMENTS_MOCK=true
# Order time-to-live before auto-expiry + refund. Use 60 in dev to see refunds fast.
ORDER_TTL_SECONDS=1200

# Stripe (test mode) — cards. Unset -> `card` deposits use the mock provider.
# https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=
# https://dashboard.stripe.com/test/webhooks (or `stripe listen` in dev) — the
# endpoint is POST {API_PUBLIC_URL}/api/v1/webhooks/payments/stripe
STRIPE_WEBHOOK_SECRET=

# NowPayments (sandbox) — crypto. Unset -> `crypto_usdt` deposits use the mock
# provider. https://account.nowpayments.io/api-keys (sandbox: account-sandbox.nowpayments.io)
NOWPAYMENTS_API_KEY=
# IPN secret from the same dashboard, used to verify the x-nowpayments-sig header.
NOWPAYMENTS_IPN_SECRET=
NOWPAYMENTS_SANDBOX=true

# Korapay (test mode) — Nigerian cards / bank transfer / USSD. Unset ->
# `korapay` deposits use the mock provider. Amounts are converted USD->NGN
# using Settings.usdToNgnRate (admin panel) before the charge is created,
# since Korapay only settles in Naira. https://dashboard.korapay.com (Settings > API Keys)
KORAPAY_SECRET_KEY=

# Cryptomus — a second crypto processor. Unset -> `cryptomus` deposits use
# the mock provider. https://app.cryptomus.com (Settings > API)
CRYPTOMUS_MERCHANT_ID=
CRYPTOMUS_API_KEY=

# Public base URL of this API (not the web app) — used to build the
# NowPayments/Korapay/Cryptomus webhook callback URLs. Needs to be reachable
# from the internet in dev (e.g. an ngrok/cloudflared tunnel) for real
# webhook delivery to work.
API_PUBLIC_URL=http://localhost:4000

# --- Workers ---
WORKERS_ENABLED=true
